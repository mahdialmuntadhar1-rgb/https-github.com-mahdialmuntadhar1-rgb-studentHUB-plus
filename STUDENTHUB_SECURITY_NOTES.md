# StudentHUB Security Notes

Admin endpoints must require backend authentication.

Public feed must show approved opportunities only.

The scraper must never fabricate opportunities when sources are silent/offline.

Never commit:

- Gemini API key
- JWT secret
- admin token
- email provider keys
- database credentials
